[workspace]
# This workspace still contains legacy/native prototype crates. The current
# product path is apps/web + apps/daemon + apps/server; do not use a full
# workspace build as current product evidence.
members = [
    "apps/daemon",
    "crates/identity",
    "crates/pairing",
    "crates/crypto",
    "crates/protocol",
    "crates/transport",
    "crates/storage",
    "crates/core",
    "apps/engine",
    "apps/cli",
]
exclude = [
    "apps/desktop-tauri/src-tauri",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
publish = false

[workspace.lints.rust]
unsafe_code = "forbid"

[profile.release]
incremental = false
debug = 1
strip = "symbols"
panic = "abort"
codegen-units = 1

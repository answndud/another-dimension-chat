[package]
name = "another-dimension-identity"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[dependencies]
ed25519-dalek = { version = "2.2.0", default-features = false, features = ["zeroize"] }

[features]
dev-insecure = []

[lints]
workspace = true
